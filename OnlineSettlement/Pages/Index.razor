@page "/"
@inject NavigationManager Nv

<h1></h1>
@code{
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        //return base.OnAfterRenderAsync(firstRender);
        Nv.NavigateTo("/manage/manage-debit", true);
    }
}
